import { ChatPanel } from "@/components/chat-panel";

export function CommandCenter() {
  return (
    <main className="h-[100dvh] min-h-0 overflow-hidden bg-black">
      <ChatPanel />
    </main>
  );
}
