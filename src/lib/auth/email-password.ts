/**
 * Local email/password sign-in for this app's Better Auth database.
 *
 * Enabled for the Login + Role & Permission flow so the login screen can offer
 * Google, email, and X in the same style as the reference UI.
 */
export const emailAndPasswordEnabled = true;
